# Production Environment Configuration
NODE_ENV=production
PORT=3000

# Database (Update with production credentials)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pokedex?schema=public"

# CORS (Specify allowed origins in production)
CORS_ORIGIN=https://your-frontend-domain.com

# Seeding - Set to false in production to download and serve images locally
# Set to true if using a CDN or external image hosting
SKIP_IMAGE_DOWNLOAD=false
