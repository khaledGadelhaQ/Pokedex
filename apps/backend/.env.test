# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Database (Use a separate test database)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/pokedex_test?schema=public"

# CORS
CORS_ORIGIN=*

# Seeding - Always skip downloading images in test environment
SKIP_IMAGE_DOWNLOAD=true
