{
  "name": "pokedex-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack Pokédex application with Nest.js backend and Vue.js frontend",
  "scripts": {
    "backend:dev": "pnpm --filter backend dev",
    "backend:build": "pnpm --filter backend build",
    "backend:start": "pnpm --filter backend start",
    "backend:start:prod": "pnpm --filter backend start:prod",
    "backend:test": "pnpm --filter backend test",
    "backend:test:e2e": "pnpm --filter backend test:e2e",
    "backend:seed": "pnpm --filter backend seed",
    "backend:import-pokemon": "pnpm --filter backend import-pokemon",
    "frontend:dev": "pnpm --filter frontend dev",
    "frontend:build": "pnpm --filter frontend build",
    "frontend:preview": "pnpm --filter frontend preview"
  },
  "keywords": [
    "pokedex",
    "nestjs",
    "vue",
    "monorepo",
    "pnpm"
  ],
  "author": "Khaled Gadelhaq",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
